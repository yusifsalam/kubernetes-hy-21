apiVersion: v1
kind: Service
metadata:
  name: kube-project-svc
spec:
  type: NodePort
  selector:
    app: kube-project
  ports:
    - name: http
      nodePort: 30080
      protocol: TCP
      port: 1234
      targetPort: 3000